<div class="topic-new-wrapper" >
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">スポットを投稿する</h1>
        <%= form_for @spot do |f| %>
          <div class="form-group">
            <%= f.label :spot_name %>
            <%= f.text_field :spot_name, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :spot_position %>
            <%= f.text_field :spot_position, class: 'form-control' %>
          </div>

          <%= f.submit '投稿', class: 'btn btn-black btn-block' %>

        <% end %>
      </div>
    </div>
  </div>
</div>
