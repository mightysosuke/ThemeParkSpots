<div class="container mt-4">
  <h2>プロフィールを変更する</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="form-group row">
      <%= f.label :account_id, class: "col-md-2 col-form-label" %><br />
      <%= f.text_field :account_id, autofocus: true, autocomplete: "account_id", class: "col-md-7" %>
    </div>

    <div class="form-group row">
      <%= f.label :email, class: "col-md-2 col-form-label" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "col-md-7" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="form-group row">
      <%= f.label :password, class: "col-md-2 col-form-label" %> <!--(パスワードを変更しない場合は空欄にしてください)<br /> -->
      <%= f.password_field :password, autocomplete: "new-password", class: "col-md-7" %>
      <!-- <% if @minimum_password_length %>
        <br />
        <em><%= @minimum_password_length %> 文字以上</em>
      <% end %> -->
    </div>

    <div class="form-group row">
      <%= f.label :password_confirmation, class: "col-md-2 col-form-label" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "col-md-7" %>
    </div>

    <div class="form-group row">
      <%= f.label :current_password, class: "col-md-2 col-form-label" %> <!-- (変更を完了するために、現在のパスワードを入力してください)<br /> -->
      <%= f.password_field :current_password, autocomplete: "current-password", class: "col-md-7" %>
    </div>

    <div class="actions">
      <%= f.submit "更新する", class: "btn btn-orange" %>
    </div>
  <% end %>

  <br/>
  <h2>アカウントを削除する</h2>

  <p>こちらのボタンで削除できます <%= button_to "アカウントを削除する", registration_path(resource_name), data: { confirm: "本当にアカウントを削除してもよろしいですか？" }, method: :delete, class: "btn btn-danger" %></p>

  <%= link_to "前の画面に戻る", :back %>
</div>
